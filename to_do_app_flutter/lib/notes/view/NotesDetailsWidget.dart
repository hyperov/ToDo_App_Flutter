import 'package:flutter/material.dart';

class NotesDetailsWidget extends StatelessWidget {
  const NotesDetailsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
